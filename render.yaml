services:
  - type: web
    name: cicd-demo
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    autoDeploy: false # deploys are triggered explicitly by the GitHub Actions pipeline
